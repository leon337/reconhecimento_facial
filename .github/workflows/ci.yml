name: CI

on:
  push:
    branches:
      - main
      - "ptp-rf-*"
  pull_request:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - name: Instalar dependências do sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake libopenblas-dev liblapack-dev postgresql-client

      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Verificar sintaxe
        run: python -m compileall -q main.py app tests scripts

      - name: Executar testes
        env:
          SECRET_KEY: ci-test-secret
        run: python -m pytest -q --junitxml=pytest-report.xml

      - name: Validar Compose de produção
        env:
          SECRET_KEY: ci-secret
          BIOMETRIC_ENCRYPTION_KEY: ci-biometric-key
          POSTGRES_DB: controle_ponto
          POSTGRES_USER: controle_ponto
          POSTGRES_PASSWORD: ci-password
        run: docker compose -f docker-compose.production.yml config --quiet

      - name: Publicar relatório de testes
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-report
          path: pytest-report.xml
          if-no-files-found: error
          retention-days: 7

  docker-build:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validar build Docker
        run: docker build --tag reconhecimento-facial:ci .
